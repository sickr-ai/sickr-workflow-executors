"""Private SICKR executor library.

The package is bundled into the orchestrator artifact at a pinned Git commit.
Executor contracts are exposed through the same immutable manifest boundary
used by organization and team executor installations.
"""

